<div class="container">
  <h1>{{beer.name}}</h1>

  <img src="{{beer.image}}" alt="{{beer.name}}" style="height:200px" />

  <p><strong>ID:</strong> {{beer.id}}</p>
  <p><strong>Precio:</strong> {{beer.price}}</p>

  <p><a href="/beers">← Volver al listado</a></p>
</div>