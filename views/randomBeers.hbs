<div>
    {{#each myRandomBeer}}
    <h2>{{name}}</h2>
    <img src="{{image_url}}" alt="">
    <p>Description: {{description}}</p>
    <p>Tagline: {{tagline}} </p>
    <p>{{food_pairing}}</p>
    <p>{{brewers_tips}}</p>

    {{/each}}
</div>