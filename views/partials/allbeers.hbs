<div class="col-md-4">
    <div class='beersList'>

        <a id='beerLink' title="{{name}}" href="/beer/beer-{{id}}"><img src="{{image_url}}" alt="{{name}}"></a>
        <article class="beerinfo">
            <h4>{{name}}</h4>
            <h5>{{tagline}}</h5>
            <p>{{description}}</p>
            
        </article>

    </div>

</div>