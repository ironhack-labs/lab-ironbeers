<div class="col-md-8">

    <article class="shownBeer">
        <img src="{{this.image_url}}">
        <div>
            <h5>{{this.name}}</h5>
            <p id="tagline">{{this.tagline}}</p>
            <p>{{this.description}}</p>
        </div>
    </article>

</div> 