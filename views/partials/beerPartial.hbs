    <div>
        <div><img src={{beer.image_url}} alt={{beer.name}}></div>
        <div>
            <h3>{{beer.name}}</h3>
            <h4>{{beer.tagline}}</h4>
            <p>{{beer.description}}</p>
        </div>
    </div>