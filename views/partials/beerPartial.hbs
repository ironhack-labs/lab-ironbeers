<div class = "beers">
    {{#each beersFromApi}}
        
        <img src="{{this.image_url}}" alt="Beer Photo">
        <p>name: {{this.name}}</p>
        <p>description :{{this.description}}</p>
        <p>tagline :{{this.tagline}}</p>

    {{/each}}
</div>