<div class="col-md-4">
    <div class="card">
        <img class="card-img-top" src={{image_url}} alt={{name}} />
        <div class="card-body">
            <h5 class="card-title">{{name}}</h5>
            <p id="greyP">{{tagline}}</p>
            <p class="card-text">{{description}}</p>
        </div>
    </div>
</div>