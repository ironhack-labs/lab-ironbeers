<div class="beer-card">
    <div class="beer-img"><img src="{{image_url}}" alt="{{tagline}}"></div>
    <div class="beer-title">
        <h1>{{name}}</h1>
    </div>
    <div class="beer-tagline">
        <h3>{{tagline}}</h3>
    </div>
    <div class="beer-text">
        <p> {{description}}</p>
    </div>

</div>