<div class="beers-partial">
    <a href="/beers/{{id}}">
    <img src="{{image_url}}" alt="">
    </a>
    <div class='partial-text'>
        <h2>{{name}}</h2>
        <p class='tagline'>{{tagline}} </p>
        <p> {{description}} </p>
    </div>
</div>