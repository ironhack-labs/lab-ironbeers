<div class="container">
    <div class="row">
        <div class="col-md-3"> 
            <article>
                    <img src="{{image_url}}" />
                
                    <p>Name: {{name}}</p>
                    <p>Description: {{description}}</p>
                    <p>Tagline: {{tagline}}</p>

            </article>
        </div>
    </div>
</div>