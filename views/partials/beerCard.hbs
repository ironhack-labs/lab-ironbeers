<div class="wrapper">
    <div class="img-div">
        <img class="img-beer"
        src="{{ image_url }}" alt="beer_{{name}}" />
    </div>
    <div class="info">
        <h2 class="b-name">{{name}}</h2>
        <h3 class="b-tagline">{{name}}</h3>
        <p class="b-description">{{ description }}</p>
    </div>
</div>