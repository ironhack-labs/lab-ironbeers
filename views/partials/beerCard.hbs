<div class="col-md-4">
    <img class="card-img-top"
        src="{{ image_url }}" alt="beer_{{name}}" />
    <div class="card-body">
        <h5 class="card-title">{{ name }}</h5>
        <h4 class="card-title card-tagline">{{ tagline }}</h4>
        <p class="card-text">{{ description }}</p>
    </div>
</div>