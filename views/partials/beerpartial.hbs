
    
    <div>
        <img src="{{this.image_url}}">
        <a href="/beers/beer?id={{this.id}}">{{this.name}}</a>
        <p>{{this.tagline}}</p>
        <p>{{this.description}}</p>
    </div>
    
