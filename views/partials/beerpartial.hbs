<img src={{this.image_url}} alt={{this.name}}>

<div>
    <h2>Name: <a href="/beers/{{this.id}}">{{this.name}}</a></h2>     
    <p>Description: {{this.description}}</p>
    <span>Tagline: {{this.tagline}}</span>
</div>
