    <div>
        <a href="/beers/{{id}}">
            <img src="{{image_url}}" alt="{{name}} image">
            <h1>{{name}}</h1>
            <h2>{{tagline}}</h2>
            <p>{{description}}</p>
        </a>
    </div>