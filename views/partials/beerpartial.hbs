

        <div class="beer">
            <div class="beer-img">
            <img src="{{this.image_url}}" alt="{{this.name}}">
            </div>
            <h1>{{this.name}}</h1>
            <h3>{{this.tagline}}</h3>
            <p>{{this.description}}</p>
        </div>

