<head>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<div class="beer-information">
    <img class='beer-picture' src="{{image_url}}" alt="{{name}} image">
    <div class='beer-information-text'>
        <a href="/beers/{{id}}"><h1>{{name}}</h1></a>
        <h2>{{tagline}}</h2>
        <p>{{description}}</p>
    </div>
</div>