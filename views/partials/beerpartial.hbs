<div class="card">
    <div class="card-img">
        <img src="{{this.image_url}}" alt="{{this.name}}">
    </div>
    <div class="card-info">
        <h2>{{this.name}}</h2>
        <p class="tagline">{{this.tagline}}</p>
        <p class="desc">{{this.description}}</p>
    </div>
</div>