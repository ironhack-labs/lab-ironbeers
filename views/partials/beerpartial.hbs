<section class="beerContainer">
    <article>
        <div id="imageBeerContainer">
            <img class="beer-api" src="{{ this.image_url }}">
        </div>
            <h3>{{ this.name }} </h3>
                <p> {{ this.tagline }} </p>
                <p> {{ this.description }} </p>
        <br>
            <hr />
        <br>
    </article>
</section>