
<div class="card border-0" style="width: 28rem;">
    <img class="card-img-top" src="{{beer.image_url}}" alt="img for {{beer.name}}">
    <div class="card-body">
        <h5 class="card-title">{{beer.name}}</h5>
        <p class="card-text">{{beer.description}}</p>
        <a href="/beers/{{beer.id}}" class="btn btn-primary">More info 🏭</a>
    </div>
</div>