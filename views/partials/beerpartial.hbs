    <div>
      <img src='{{image_url}}' alt='{{name}}' class='beer-img' />
    </div>

    <div>
      <p>{{name}}</p>
    
      <p>{{description}}</p>

      <p>{{tagline}}</p>
      
    </div>


{{!--     {{#each pokemon}}
    {{> pokeCard}}
  {{/each}} --}}

  

