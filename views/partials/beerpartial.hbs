<section class="container">
    <article>
        <img class="beer-pic" src="{{ this.image_url }}">
        <h3>{{ this.name }} </h3>
        <p> {{ this.tagline }} </p>
        <p> {{ this.description }} </p>
        <br>
        <hr />
        <br>
    </article>
</section>