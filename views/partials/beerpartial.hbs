<div class="container">
    <div class="col">
        <div class="card">
            <img src={{this.image_url}} />
            <div class="card-body">
                <h5 class="card-title">{{this.name}}</h5>
                <h6>{{this.tagline}}</h6>
                <p class="card-text">{{this.description}}</p>
            </div>
        </div>
    </div>
</div>