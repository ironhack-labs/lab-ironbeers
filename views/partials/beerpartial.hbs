    {{#each beers}}
    <div>
      
        <img src="{{this.image_url}}" alt="">
        <a href="/beers/{{this.id}}">{{this.name}}</a>
        {{this.description}}
        {{this.tagline}}
     
    </div>
    {{/each}}