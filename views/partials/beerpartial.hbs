    
        <div class="element-image">
        <img class="beer-image" src="{{image_url}}" alt="beer">
        </div>

        <div class="element-description">  
	    <h3>{{name}}</h3>
        <h4>{{tagline}}</h4>
        <p>{{description}}</p>
        </div>  
    
