<div class="aux col-md-4">
    <img src={{image_url}} alt="beer">
    <div class="beer-card">
        <a href='/beers/{{id}}'>{{name}}</a>
        <h6>{{tagline}}</h6>
        <p>{{description}}</p>
    </div>
</div>