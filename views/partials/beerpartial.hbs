<div class="container ">
    <div class=" row ">
        <div class="col-md  ">
            <img src="{{image_url}}" class="card-img-top beer-img" alt=" {{name}}">
            <div>
                <a href="/beer/{{id}}">
                    <h5 class="card-title fw-bolder ">{{name}}</h5>
                </a>
                <p class="text-secondary card-text">{{tagline}}</p>
                <p class="card-text"> {{description}}</p>
            </div>

        </div>
    </div>
</div>