<div>
    <div>
        <a href="/beer/{{this.id}}"><img src={{this.image_url}}></a>
    </div>
    <div>
        <p>{{this.name}}</p>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    </div>
</div>