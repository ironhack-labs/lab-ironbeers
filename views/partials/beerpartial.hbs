<div>
    <div class="col-md-4">
        <article class="player-card">
            <img src="{{image_url}}" alt="{{name}}">
            <p>{{name}}</p>
            <p>{{description}}</p>
            <p>{{tagline}}</p>

        </article>
    </div>
</div>