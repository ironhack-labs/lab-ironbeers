{{!-- <div class="card">
  <div class="row no-gutters">
    <div class="col-md-4" style="max-width: 60px">
      <img src="{{this.image_url}}" alt="{{this.name}}" class="card-img"/>
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h3 class="card-title">{{this.name}}</h3>

        <p class="card-text"><small class="text-muted">Hello My Name is {{this.name}}</small></p>

        <p class="card-text">{{this.description}}</p>
        <p class="card-text">{{this.tagline}}</p>

        <h5>Food Pairing</h5>

    <ul>
        {{#each this.food_pairing}}
        <li>{{this}}</li>
        {{/each}}

    </ul>
        <p><strong>Brewer tips:</strong> {{this.brewers_tips}}</p>
      </div>

    </div>
  </div>
</div> --}}