<article class="partial-beers">
    <div class="card-img">
        <img class="d-block" src="{{image_url}}" alt="{{name}}">
    </div>
    <h2>{{name}}</h2>
    <h3>{{tagline}}</h3>
    <p>{{description}}</p>
</article>