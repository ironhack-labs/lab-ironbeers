<div class="beer-card">
    <img src="{{this.image_url}}" alt="">
    <div>
        <h5>{{this.name}}</h5>
        <p class = "card-text">{{this.description}}</p>
        <p>{{this.tagline}}</p>
    </div>
</div>

