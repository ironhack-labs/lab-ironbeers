<div class="container">
    <div class="row">
        {{#each beers}}
        <div class="col-4 pt-5">
            <div class="row justify-content-center pb-3">
                <img src="{{image_url}}" alt="Beer {{id}}" style="height: 120px; width:auto">
            </div>
            <div>
                {{>beerpartial}}
            </div>
        </div>
        {{/each}}
    </div>
</div>