{{#each beersFromApi }}
    <div><img class="beer-pic" src="{{ this.image_url }}"></div>
    <div> {{ this.name }} </div>
    <div> {{ this.description }} </div>
    <div> {{ this.tagline }} </div>
    <br>
    <hr/>
    <br>
{{/each}}