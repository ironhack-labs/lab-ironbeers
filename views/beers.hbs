<h1>Beers</h1>
<ul>
    {{#each beersFromApi as | beer | }}

    <div>
        <img src="{{beer.image_url}}" alt="beerImg">
        <h1>{{beer.name}}</h1>
        <h4>{{beer.tagline}}</h4>
        <p>
            {{beer.description}}
        </p>
    </div>

    {{/each}}

</ul>