<h1>Beers</h1>

{{#each beersFromApi}}
    <ul>
        <li>{{this.name}}</li>
        <img src="{{this.image_url}}" alt="">
        <li>{{this.description}}</li>
        <li>{{this.tagline}}</li>
    </ul>
{{/each}}