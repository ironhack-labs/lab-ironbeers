{{#each beers}}
    <div class="container">
        <img src={{this.image_url}} />
        <h1>{{this.name}}</h1>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    </div>
{{/each}}