<div class="row">

    {{#each beers}}

    <div class="col-4 md-4">

        <div class="card">
            <img src="{{image_url}}" alt="{{name}}">
            <p>{{name}}</p>
            <p>{{description}}</p>
            <p>{{tagline}}</p>
        </div>
    </div>
    {{/each}}
</div>