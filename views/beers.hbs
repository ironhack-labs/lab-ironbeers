<div class="beers">
    {{#each beers}}
    <img src="{{image_url}}">
    <div class="beersText">
        <h1>{{name}}</h1>
        <p>{{description}}</p>
        <p>{{tagline}}</p>
    </div>
    {{/each}}
</div>