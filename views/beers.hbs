<ul>
  {{#each beersFromApi}}
    <li>{{this.name}}</li>
    <li>{{this.image_url}}</li>
    <li>{{this.description}}</li>
    <li>{{this.tagline}}</li>
    <img src="{{this.image_url}}" alt="Beer">
  {{/each}}
</ul>

