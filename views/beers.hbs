{{!-- <div class="row">
    {{#each beers}}
    <div class="col-4">
        <img class="beers-image" src="{{image_url}}" />
        <h5 class="beers-title">{{name}}</h5>
        <p class="beers-tagline">{{tagline}}</p>
        <p class="beers-description">{{description}}</p>
    </div>
    {{/each}}
</div> --}}
<div class="row">
    {{#each beers}}
    <div class="col-4">
        <a href="/beers/beer-{{id}}" class="beer-specific" value="{{id}}">
            {{> beerpartial this}}
        </a>
    </div>
    {{/each}}
</div>