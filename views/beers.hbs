<div>
    {{#each data}}
        <div>
            <img src="{{this.image_url}}" alt="beer-picture">
            <h3>{{this.name}}</h3>
            <h4>{{this.tagline}}</h4>
            <p>{{this.description}}</p>
        </div>
    {{/each}}
</div>