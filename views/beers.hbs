<div class="container ">
    <h1>Beers in stock</h1>
    <div class="row">
        {{#each arrBeer}}
        <div class="col-md-6">
            {{> partialBeer this}}
        </div>

        {{/each}}
    </div>
</div>