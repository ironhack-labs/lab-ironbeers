{{!-- <div class="container">
  <h1>Beers</h1>
  <div class="row">
      {{#each beers}} 
        <img src="{{this.image_url}} " alt="">
        <h4>{{this.tagline}}</h4>
        <h3>{{this.name}} </h3>
        <p>{{this.description:}} </p>
      {{/each}}
  </div>
</div> --}}

<div class="container">
    <div class="col-4">
        {{#each beers}} 
            <div class="card">
                <img class="card-img-top" src="{{this.image_url}}" alt="{{this.name}}"/>
                <div class="card-body">
                <h5 class="card-title">{{this.name}}</h5>
                <h6class="card-title">{{this.tagline}}</h6>
                <p class="card-text">{{this.description}}</p>
                </div>
            </div>
    {{/each}}
    </div>
</div>