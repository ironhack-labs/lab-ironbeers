<h1>List of Beers</h1>

{{#each beers}}
  <div>
    <h2>{{name}}</h2>
    <img src='{{image_url}}' alt='{{name}} Image' class='beer-images' />
    <p>{{description}}</p>
    <p>Tagline:{{tagline}}</p>
  </div>
{{/each}}