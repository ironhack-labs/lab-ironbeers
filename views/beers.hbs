
{{#each beer}}
<div class="contenedor">
    <img src="{{this.image_url}}" alt="{{this.name}} photo">
    <div class="beertext">
    <div class="name"> {{this.name}} </div>
    <div class="description"> {{this.description}} </div>
    <div class="tagline"> {{this.tagline}} </div>
    </div>
</div>
{{/each}}