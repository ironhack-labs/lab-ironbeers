<div>

      {{#each beersFromApi}}

      <img src="{{image_url}}" alt="">
      <p>{{name}}</p>
      <p>{{description}}</p>
      <p>{{tagline}}</p>
      {{else}}
        <p>There are no beers yet!</p>
      {{/each}}

    
  
</div>