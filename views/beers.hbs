<div class="container">
    <div class="row d-flex">
        {{#each beersFromApi}}
        <div class="col-3">
            <img src={{image_url}} class="card-img-top  row justify-content-center" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
                <p>{{tagline}}</p>
                <p class="card-text">{{description}}</p>
            </div>
        </div>
        {{/each}}
    </div>
</div>