<main>

{{#each beers}}
 
  <div class="beers">
    <img src="{{this.image_url}}" alt="">
    <h2>{{this.name}}</h2>
    <p>Description: {{this.description}}</p>
    <p>Tagline: {{this.tagline}}</p>
  </div>

{{/each}}

</main>