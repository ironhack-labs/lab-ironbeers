
    {{#each content}}
        <div class="beers">
            <h3>{{this.name}}</h3>
            <h4>{{this.tagline}}</h4>
            <p>{{this.description}}</p>

            <img src="{{this.image_url}}" alt="">
            
        </div>
    {{/each}}    
