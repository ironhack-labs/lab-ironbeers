{{#each beers}}
    <div>
        <p>
            <img src="{{this.image_url}}" alt="Beer">
        </p>
        <p>Name of the beer: {{this.name}}</p>
        <p>Tagline: {{this.tagline}}</p>
        <p>Description: {{this.description}}</p>
    </div>
{{/each}}