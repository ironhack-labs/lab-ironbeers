
    <div class="container">
        <h1>Beers</h1>
        <div class="row">
            {{#each selectBeer}}
             <img src="{{this.image_url}}">
             <h3>{{this.name}}</h3>
             <p>{{this.description}}</p>
             <p>{{this.tagline}}</p>
             {{/each}}
        </div>
    </div>

