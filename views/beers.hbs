<section class="container">

  {{#each beers}}
    <img src="{{this.image_url}}" alt="Beer">
    <p>{{this.name}}</p>
    <p>{{this.description}}</p>
    <p>{{this.tagline}}</p>
  {{/each}}


</section>