<h1>All beers</h1>

<div class="all-beers">
    {{#each beers}}
        <div class="more-info">
        {{> beerpartial}}
        <a href="/beers/{{this.id}}" class="btn btn-primary">More info</a>
        </div>
    {{/each}}
</div>