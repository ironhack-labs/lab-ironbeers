{{! Display name, image and description and tagline }}

<ul>
  {{#each beers}}
    <h1>{{this.name}} </h1>
    <img src={{this.image_url}} />
    <p> {{this.description}}</p>
    <p>{{this.tagline}}</p>

  {{/each}}
</ul>