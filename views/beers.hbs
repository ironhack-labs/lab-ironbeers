{{#if beersFromApi}}

  <ul>
    {{#each beersFromApi}}
      <li>{{this.name}}<img
          src={{this.image_url}}
        />:{{this.description}}:{{this.tagline}}</li>
    {{/each}}
  </ul>
{{/if}}