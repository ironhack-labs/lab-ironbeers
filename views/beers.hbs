<div class="container">
  <h1>Beers</h1>
  <div class="row">
    <ul>
      {{#each beersFromApi}} 
      <li><img src={{this.image_url}}></li>
      <li>{{this.name}}</li>
      <li>{{this.description}}</li>
      <li>{{this.tagline}}</li>
      {{/each}}
    </ul>
  </div>
</div>