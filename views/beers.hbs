<h1>Beers</h1>


  <div class="container">
    <div class='row'>
        {{#each beersArr}}
            <div class="card theBeer" style="width: 18rem;">
                <img src={{this.image_url}} class="card-img-top" alt={{this.name}}>
                 <div class="card-body">
                     {{> beerpartial}}
                </div>
            </div>
         {{/each}}     
    </div>
</div>

