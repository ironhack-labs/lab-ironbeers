<div class="container">
  <h1>Beers</h1>
  <div class="row">
      {{#each beersFromApi}}
      <img class="img-beers" src={{this.image_url}} alt="Beers">
     <h3> {{this.name}} </h>
     <p>{{this.tagline}}</p>
     <p>{{this.description}}</p>
      {{/each}}
  </div>
</div>