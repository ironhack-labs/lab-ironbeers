<div>
    <h1>Beers</h1>
    {{#each data}}
        <div>
            <img src="{{this.image_url}}" alt="beer-img">
            <h2>{{this.name}}</h2>
            <h3>{{this.tagline}}</h3>
            <p>{{this.description}}</p>
        </div>
    {{/each}}
</div>