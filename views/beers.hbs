
<h1 class="h1 text-center">Beers</h1>

<div class="container">
    <div class="row">
        {{#each beers }}
        <div class="col-6">
           <p>{{this.name}}</p>
        {{> beerpartial }}
        </div>
        {{/each}}
    </div>
</div> 