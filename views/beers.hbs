<h1>All the beers</h1>
<div class="beerWrapper">
    {{#each apiBeers}}
<ul>
    <li><img src="{{image_url}}" alt="Bottle of {{name}}" class="beerImg"/></li>
    <li>{{name}}</li>
    <li>{{description}}</li>
    <li class="tagline">{{tagline}}</li>
</ul>
{{/each}}
</div>