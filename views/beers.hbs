


<ul>
    {{#each beers as |beer|}}
      <li>
        {{beer.name}}
        <img src={{beer.image_url}}/>
        {{beer.name}}
        {{beer.description}}
        {{beer.tagline}}
      </li>
        {{/each}}
  </ul>