<div class="container">

    <div class="row">

        {{#each beersInfo}}
        <div class="col-md-4">
            <article class="beer-card">
                <img src="{{image_url}}" alt="{{name}} {{tagline}}">
                <p class="title"><strong>{{name}}</strong></p>
                <p class="tagline"> {{tagline}} </p>
                <p>{{description}}</p>
            </article>
        </div>
        {{/each}}

    </div>

    <hr>

    <a href="/" class="btn btn-dark mt-3 mb-5">Back</a>

</div>