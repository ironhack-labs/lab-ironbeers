<section class="container">
{{#each beersFromApi}}
    <a class="beer" href="/beer-info/{{this.id}}">
        <img class="beer-img"src="{{this.image_url}}">
        <h2>{{this.name}}</h2>
        <h3>{{this.tagline}}</h3>
        <p>{{this.description}}</p>
    </a>
{{/each}}
</section>