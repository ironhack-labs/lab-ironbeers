<div class='container'>

  <div class='row'>
    {{#each beersFromApi}}
      <img src={{image_url}} alt='' />
      <h2>{{name}}</h2>
      <p>{{description}}</p>
      <p>{{tagline}}</p>
      <a href='/beerpartial'>Partial Beer</a>
    {{/each}}
  </div>
</div>