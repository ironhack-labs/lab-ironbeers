{{#each beers}}
  <div class="beer-image">
    <img src='{{image_url}}' />
  </div>
  <div class="beer-text">
    <h1>{{name}}</h1>
    <p>{{description}}</p>
    <p>{{tagline}}</p>
  </div>
{{/each}}