<div class='beers'>
  {{#each beersFromApi}}
    <div class='individual-beers'>
      <img src='{{this.image_url}}' alt='{{this.name}}' />
      <h3>{{this.name}}</h3>
      <h4>{{this.description}}</h4>
      <p>{{this.tagline}}</p>
    </div>
  {{/each}}
</div>