<div>
    {{#each beers}}
    <div>
        <img src={{image_url}} alt={{beer img}}/>
        <a href='/product-details'>{{name}}</a>
        <h4>{{tagline}}</h4>
        <p>{{description}}</p>
    </div>
    {{/each}}
</div>