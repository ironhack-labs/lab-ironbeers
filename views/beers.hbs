<div class="container">
    <div class="row">
        {{#each beer}}
        <div class="col-md-4">
            <article>
                {{>beerpartial}}
            </article>
        </div>
        {{/each}}
    </div>