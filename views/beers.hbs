<div class='container'>
    {{#each beerArr}}
        <img src="{{this.image_url}}" width='100px'>
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    {{/each}}

</div>

