<div class="container mb-auto my-12 mx-auto px-4 md:px-12">
    <div class="flex flex-wrap -mx-1 lg:-mx-4">
        {{#each beers}}
            {{> beerpartial this}}
        {{/each}}
    </div>
</div>