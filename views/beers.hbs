{{#each beersFromApi}}
    <div>
        <img src="{{image_url}}" alt="">
        <h1>{{name}}</h1>
        <p>{{description}}</p>
        <h2>Food Pairing</h2>
        <ul>
            <li>{{food_pairing}}</li>
        </ul>
        <h3>Brewers Tips: </h3>
        <p>{{brewers_tips}}</p>
    </div>
{{/each}}