<section>
        {{#each this}}
            <img src="{{image_url}}" alt="">
            <h3>{{name}}</h3>
            <p>{{description}}</p>
            <p>{{tagline}}</p>
        {{/each}}
</section>
