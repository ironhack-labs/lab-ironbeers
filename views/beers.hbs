{{#each cervezas}}
  <div class='cards'>
    <div class='card'>
      <img src={{this.Image_url}} class='' />
      <p>{{this.name}}</p>
      <p>{{this.tagline}}</p>
      <p>{{this.description}}</p>
    </div>
  </div>
{{/each}}