<div>
<h2>Beer Options</h2>
     {{#each beers}}
        <img src="{{this.image_url}}" alt="beers" />
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <h3>{{this.tagline}}</h3>
     {{/each}}

</div>