<section class="container">
{{#each beersFromApi}}
    <div class="beer">
        <img class="beer-img"src="{{this.image_url}}">
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <h3>{{this.tagline}}</h3>
    </div>
{{/each}}
</section>