<div class='beer-list'>
  {{#each this as |beer|}}
    <div class='beer-card'>
      <img src='{{beer.image_url}}' />
      <div class='beer-decription'>
        <h2>{{beer.name}}</h2>
        <p>{{beer.description}}</p>
        <p>{{beer.tagline}}</p>
      </div>
    </div>

  {{/each}}
</div>