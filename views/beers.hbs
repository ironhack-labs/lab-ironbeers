<div class="container">
    <div class="row">
        <div class="col-4">
            {{#each this}}
                <h5>{{name}}</h5>
                <h6>{{tagline}}</h6>
                <img src="{{image_url}}" alt="{{name}}">
                <p>{{description}}</p>
            {{/each}}
        </div>
    </div>
</div>



