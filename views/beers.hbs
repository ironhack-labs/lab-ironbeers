<h1>Beers</h1>

{{#each beers}}
<li class="item">
    <img src="{{this.image_url}}" alt="{{this.name}}">
    <h2 class="title">{{this.name}}</h2><br>
    <p>{{this.description}}</p>
    <p>{{this.tagline}}</p>
</li>
{{/each}}