<h1>The Beer List</h1>

<div>
  {{#each beers}}
    <div >
      <img src="{{this.image_url}}" />
      <h2>{{this.name}}</h2>
      <h3>{{this.tagline}}</h3>
      <p>{{this.description}}</p>
    </div>
  {{/each}}
</div>