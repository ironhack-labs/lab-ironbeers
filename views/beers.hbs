{{#each beersFromApi as |beer|}}
    {{> beerpartial
    id = beer.id
    name = beer.name
    image_url = beer.image_url
    tagline = beer.tagline
    description = beer.description
    }}
{{/each}}