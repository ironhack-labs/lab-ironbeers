<div>
<ul>
    <% if (beers) { %>
      <% beers.forEach(beer => { %>
      <li><%= beer.name %> <br><%= beer.description %><br><%= beer.image_url %><br><%= beer.tagline %></li>
      <% }) %>
    <% } %>
  </ul>
</div>