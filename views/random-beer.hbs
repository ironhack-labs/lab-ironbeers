<div class="container">

    {{#each beersInfo}}

    <img src="{{image_url}}" alt="beerImage">

    <div class="container">

        <h3>{{name}}</h3>

        <p>{{description}}</p>

        <span>{{tagline}}</span>

    </div>

    {{/each}}

</div>