<div class="container">
    <h1> hola</h1>
    {{#each beer}}
    <div>
        <article>
            <img src="{{image_url}}" alt="{{name}}">
            <hr>
            <p>{{description}}</p>
            <p>{{tagline}}</p>
            <p>{{food_pairing }}</p>
            <p>{{brewers_tips}}</p>
        </article>
    </div> {{/each}}
</div>