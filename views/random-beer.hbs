<div class="container">
    <div class='beer'>

         <div class="beer-img">
      <img src="{{randomBeer.image_url}}" alt="">
    </div>
    <div class="beer-info">
      {{#each responseFromAPI }}
        {{> beerpartial }}
        <p>{{ food_pairing }}</p>
        <p>{{ brewers_tips }}</p>
      {{/each }}
    </div>
  </div>
</div>
