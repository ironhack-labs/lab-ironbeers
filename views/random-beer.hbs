<div>
    <img src="{{image_url}}" alt="{{name}}">
    <div>
        <h1>{{name}}</h1>
        <p>{{tagline}}</p>
        <p>{{description}}</p>
        <h2>Food Pairing</h2>
        <ul>
            {{#each food_pairing}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
        <p><b>Brewers Tips:</b> {{brewers_tips}}</p>
    </div>
</div>