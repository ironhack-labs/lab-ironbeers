{{!-- <div class="container">
    <div class="row">
        {{#each randomBeers}}
        {{> randomBeersList this}}
        {{/each}}
    </div>
</div> --}}


<div class="container">
  <div class="row">
      {{#each randomBeers}} 
        {{> beerpartial}} 
        <h5>Food Pairing</h5>

    <ul>
        {{#each this.food_pairing}}
        <li>{{this}}</li>
        {{/each}}

    </ul>
        <p><strong>Brewer tips:</strong> {{this.brewers_tips}}</p>
      {{/each}}
  </div>
</div>