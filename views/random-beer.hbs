{{#each randombeer}}

  <div class='container'>
    <div class='row'>
      <div class='randombeer col'>
        <img src='{{image_url}}' />
        <div class='col'></div>
        <h1>{{name}}</h1>
        <p>{{tagline}}</p>
        <p>{{description}}</p>
        <p>{{food_pairing}}</p>
        <p>{{brewers_tips}}</p>
</div>
      </div>
    </div>
  </div>
{{/each}}