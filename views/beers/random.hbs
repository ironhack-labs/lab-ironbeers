
<div>
  <img class="mx-5 rounded float-start" style="max-height: 400px;" src="{{beer.image_url}}" alt="beer"/>
  <h1>{{beer.name}}</h1>
  <p>{{beer.description}}</p>
  <h4>Food Pairing</h4>
  <p>{{beer.food_pairing}}</p>
  <h4>Brewer Tips</h4>
  <p>{{beer.brewers_tips}}</p>
</div>