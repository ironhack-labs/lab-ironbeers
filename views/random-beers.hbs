<main class="container ">
    <div class="row d-flex justify-content-center">
        <div class="col">
            <div class="row">
                {{#each beersFromApi}}
                {{>randomBeersCard this }}
                {{/each}}
            </div>

        </div>
    </div>



</main>