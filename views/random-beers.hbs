{{#each beer}}
    <img src="{{image_url}}" alt="{{name}}">
    <h1>{{name}}</h1>
    <p>{{description}}</p>
    <p>{{tagline}}</p>

    <ul>
        {{#each food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>

    <p>{{brewers_tips}}</p>
{{/each}}