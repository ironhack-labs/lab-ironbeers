<div class="container">

    <img src="{{image_url}}" alt="">
    <h1>{{name}}</h1>
    <p>{{description}}</p>
    <h3>{{tagline}}</h3>
    <ul> {{#each food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    <h6>{{brewers_tips}}</h6>
</div>
