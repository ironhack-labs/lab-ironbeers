{{#each beer}}

    <div class = "random-beer">
      <img src="{{this.image_url}}" alt="beer" width = "10%">
      <h3>{{this.name}}</h3>
      <p>{{this.tagline}}</p>
      <p>{{this.description}}</p>
    </div>

  {{/each}}