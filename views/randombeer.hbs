<article class=''>
    <div>
    
      <img src={{beer.image_url}} height="300px" />
      <h2>{{beer.name}}</h2>
      <ul>
        {{#each beer.foodparing}}
          <li> {{this}} </li>
        {{/each}}
      </ul>
      <h4>{{beer.tagline}} </h4>
      <p>{{beer.description}} </p>
    
  </div>
</article>