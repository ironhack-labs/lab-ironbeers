<article class="beer">
    <img src="{{this.image_url}}" alt="Beer image">
    <div>
       <h1>{{this.name}}</h1> 
    </div>
    <div>
        <p>{{this.description}}</p> 
    </div>
    <div>
       <p>{{this.tagline}}</p> 
    </div>
</article>

