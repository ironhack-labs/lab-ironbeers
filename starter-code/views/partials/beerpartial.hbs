<div class="columnas col-md-4">
    <div class="cervecitas">
        <img class="image" src={{this.image_url}} alt={{this.name}}>
        <h1 class="nombre"> <a href="/beer/{{this.id}}">{{this.name}}</a></h1>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    </div>
</div>