{{#each randombeers}}
<div>
<img src="{{ image_url }}">
<h1 class="title">{{ name }}</h1>
<p class="description">{{ description }}</p>
<p class="foodpairing">{{ food_pairing }}</p>
<p class="brewertips">{{ brewer_tips }}</p>

</div>

{{/each}}