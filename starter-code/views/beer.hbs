
<div class="container">
    <div class="row"></div>
    <div class="col-md-12 beer card">
    {{#each allBeers}}
        <img class="" src="{{image_url}}" alt="{{name}}">
        <div class="">
        <h4>{{name}}</h4>
        <p class="card-text">{{description}}</p>
        <small>{{tagline}}</small>
    {{/each}}
    </div>
    </div>
</div>
