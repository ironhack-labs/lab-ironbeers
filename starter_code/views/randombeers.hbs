{{#each beers}}
<div class="box">
    <figure>
      <img class="img-fig" src="{{this.image_url}}">
        </figure>
        <h2>{{this.name}}</h2>
        <h3>{{this.tagline}} </h3>
        <p>{{this.description}}</p>
    </div>
{{/each}}