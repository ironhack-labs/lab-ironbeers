<div class="beer">
    <div class="beer-img">
        <img src={{this.image_url}} alt="beer">
    </div>

    <div class="beer-content">
        <h2>{{this.name}}</h2>
        <h3>{{this.tagline}} </h3>
        <p>{{this.description}}</p>
    </div>
</div>