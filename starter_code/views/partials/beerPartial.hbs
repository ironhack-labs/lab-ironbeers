<div class="beerCards">
      <div class="beerContainer">
        <img src={{this.image_url}}>
        <h1> {{this.name}} </h1>
        <h4> {{this.tagline}} </h4>
        <p> {{this.description }} </p>
      </div>
</div>