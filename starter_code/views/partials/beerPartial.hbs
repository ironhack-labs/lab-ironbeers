<div id="beerinfo">
    <img src="{{this.image_url}}" height="30%" width="30%">
    <div>
        <h2>{{this.name}}</h2>
        <h3>{{this.tagline}}</h3>
        <p>{{this.description}}</p>
    </div>
</div>