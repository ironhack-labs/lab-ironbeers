<div class="theBeerList">
    <div class="beerInfo">
    <img src="{{this.image_url}}" alt="beer">
    <div>
    <h2>{{this.name}}</h2>
    <h3>{{this.tagline}}</h3>
    <h4>{{this.tagline}}</h4>
    </div>
    </div>
</div>