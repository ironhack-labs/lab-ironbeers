
<div class="col-md-4">
    <div class="img-beer text-center">
        <img src="{{this.image_url}}" alt="{{this.name}}" />
    </div>
    <div class="text-beer">
        <h2> {{this.name}}</h2>
        <h4> {{this.tagline}}</h4>
        <p> {{this.description}}</p>
    </div>
</div>
