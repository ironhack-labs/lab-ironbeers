<div class="beer__card">
    <div class="beer__img--wrapper">
        <img class="beer__img--img" src={{image_url}} alt="{{tagline}}">
    </div>
    <h1 class="beer__title">{{name}}</h1>
    <p class="beer__tagline">{{tagline}}</p>
    <p class="beer__description">{{description}}</p>
</div>