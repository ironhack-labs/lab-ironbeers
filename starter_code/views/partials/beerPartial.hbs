<section class = "beer-card">
    <img  class= "description-image" src="{{this.image_url}}" alt="It's a beer">
    <h1>{{this.name}}</h1>
    <h2> {{this.tagline}}</h2>
    <p>{{this.description}}</p>   
</section>
