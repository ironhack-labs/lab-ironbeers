<div class="container-beer-information">
  <img class="all-beers" src="{{this.image_url}}" />
  <div class="nombre-slogan">
    <h2>{{this.name}}</h2>
    <h3>{{this.tagline}}</h3>
  </div>
  <p>{{this.description}}</p>
</div>