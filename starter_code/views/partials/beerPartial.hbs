<div class="row">
    <div class="col">
        <img src="{{this.image_url}}" alt="Bottle of {{this.name}}">
        <div class="info">
            <h2>{{this.name}}</h2>
            <h3>{{this.tagline}}</h3>
            <p>{{this.description}}</p>
        </div>
    </div>
</div>    
