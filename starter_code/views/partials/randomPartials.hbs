<div class="random-container">
    <img class="random-img" src="{{beer.image_url}}" alt="Beer image">
    <p class="random-name">{{beer.name}}</p>
    <p>{{beer.description}}</p>
    <p>{{beer.tagline}}</p>
</div>