<div class="beers-partial">
    <div class="beers-img">
        <img src="{{this.image_url}}" alt="Beers photos">
    </div>
    <h1>{{this.name}}</h1>
    <h4>{{this.tagline}}</h4>
    <p>{{this.description}}</p>
</div>