{{!-- <div class="beer">
  <img src="{{image_url}}" width=100 >
  <h2>{{name}}</h2>
  <h3>{{tagline}}</h3>
  <p>{{description}}</p>
  <h4>Food Pairing</h4>
</div> --}}

    <div class="text-center">
      <img src="{{beers.image_url}}" width=100 >
    </div>
    <div class="random-beer-info">
      <h2>{{beers.name}}</h2>
      <h3>{{beers.tagline}}</h3>
      <p>{{beers.description}}</p>
      <p><strong>Food Pairing</strong><p>
      <ul>
        {{#each beers.food_pairing}}
          <li>{{this}}</li>
        {{/each}}
      </ul>
      <p><strong>Brewers Tips:</strong> {{beers.brewers_tips}}</p>
    </div>




{{!-- 
<div class="beer">
  <div class="text-center">
    <img src="{{image_url}}" width=100 >
  </div>
  <h2>{{name}}</h2>
  <h3>{{tagline}}</h3>
  <p>{{description}}</p>
</div> --}}