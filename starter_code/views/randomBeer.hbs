<div><img src="{{beer.image_url}}" alt=""></div>
<div>
    <h1>{{beer.name}}</h1>
    <h2>{{beer.tagline}}</h2>
    <p>{{beer.description}}</p>
    <h2>Food pairing</h2>
    <ul>
        {{#each beer.food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    <p><b>Brewers Tips: </b>{{beer.brewers_tips}}</p>
</div>