<div class="position-relative overflow-hidden p-3 p-md-5 text-left beer-box">
  <div class="col-md-5 p-lg-5 mx-auto my-5">
    <h1 class="h1 font-weight-normal text-uppercase font-weight-bold">{{beers.name}}</h1>
    <h5 class="h3 font-weight-normal text-secondary">{{beers.tagline}}</h5>
    <p class="lead font-weight-normal">{{beers.description}}</p>
  </div>
  <div class="beer-img">
      <img src="{{beers.image_url}}" alt="{{beers.name}}">
  </div>
    <div class="beer-img beer-img-2">
      <img src="{{beers.image_url}}" alt="{{beers.name}}">
  </div>
</div>