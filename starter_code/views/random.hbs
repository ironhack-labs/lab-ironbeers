{{#each beer}}
    <div class="random-beer">
        <img src="{{this.image_url}}" alt="chela">
        <div class="description">
            <h3>{{this.name}}</h3>
            <p>{{this.tagline}}</p>
            <p>{{this.description}}</p>
        </div>
    </div>
{{/each}}