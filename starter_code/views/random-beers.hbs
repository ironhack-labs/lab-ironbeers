<div class="beers-container">
    <div class="beers">
        <img src="{{image_url}}" alt="beer-img">
        <h1>{{name}}</h1>
        <span>{{tagline}}</span>
        <p>{{description}}</p>
    </div>
</div>