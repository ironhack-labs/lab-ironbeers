
<div class="container-general-random">
    <div>
        <img class="ramdon-beer" src="{{beer.image_url}}"/>
    </div>
    <div class="random-information">
        <h1>{{beer.name}}</h1>
        <h2>{{beer.tagline}}</h2>
        <p> {{beer.description}} </p>
        <p> {{beer.food_pairing}} </p>
        <p> {{beer.brewers_tips}} </p>
    </div>
</div>

