
{{#each randomBeers}}
    <h1>{{this.name}}</h1>
    <h1>{{this.tagline}}</h1>
    <h1>{{this.description}}</h1>
    <ul>
        <li>{{this.food_pairing[0]}}</li>
        <li>{{this.food_pairing[1]}}</li>
        <li>{{this.food_pairing[2]}}</li>
    </ul>
    <h1>{{this.name}}</h1>
    <h1>{{this.name}}</h1>
    <h1>{{this.name}}</h1>
    <h1>{{this.name}}</h1>
    {{/each}}