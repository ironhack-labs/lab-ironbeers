<div>
  {{!-- {{>beerPartial}} --}}
  <div class="beers-wrap">
    {{#each beers as |beer|}}

    <div class="card beer-card" style="width: 18rem;">
    <img src="{{beer.image_url}}" class="card-img-top" alt="beer image">
    <div class="card-body">
      <h5 class="card-title">{{beer.name}}</h5>
      <p class="card-text">{{beer.tagline}}</p>
      <p class="card-text">{{beer.description}}</p>
    </div>
  </div>

  {{!-- <div><img src="{{beer.image_url}}" alt="beer image"></div>
    <h1>{{beer.name}}</h1>
    <h2>{{beer.tagline}}</h2>
    <p>{{beer.description}}</p>
  </div> --}}

{{/each}};

</div>